FROM ollama/ollama:latest

RUN set -eux; \
    apt-get update; \
    apt-get install -y --no-install-recommends tini curl; \
    rm -rf /var/lib/apt/lists/*

EXPOSE 11434/tcp

ENTRYPOINT ["/entrypoint.sh"]

CMD ["your-original-binary","--flag"]